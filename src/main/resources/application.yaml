server:
  port: 8081
spring:
  application:
    name: backend
  config:
    import: application-secret.yaml
  datasource:
    url: jdbc:mysql://localhost:3306/edu4all?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: nguyencuong11112004@gmail.com
    password: fqso dhhd nqhy qkvs
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  # Cấu hình upload file multipart
  servlet:
    multipart:
      enabled: true
      max-file-size: 500MB      # Kích thước tối đa mỗi file
      max-request-size: 510MB   # Kích thước tối đa toàn bộ request

# Thư mục lưu file upload (tương đối so với thư mục chạy server)
app:
  upload-dir: uploads
