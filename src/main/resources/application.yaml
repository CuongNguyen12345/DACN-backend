server:
  port: 8081
spring:
  application:
    name: backend
  datasource:
    url: jdbc:mysql://localhost:3306/edu4all?useSSL=false&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect